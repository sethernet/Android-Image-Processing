package com.cinematic.cinematic;

import android.annotation.SuppressLint;
import android.media.effect.Effect;
import android.media.effect.EffectContext;
import android.media.effect.EffectFactory;

public class VideoEffect {
	
	//none of this is working at the moment, but this will handle the 
	//effects added to videos
	/** EffectContext effect = EffectContext.createWithCurrentGlContext();

	EffectFactory newEffect =  effect.getFactory();
	
	sepia = newEffect.createEffect(EFFECT_SEPIA).setParameter(null);
	
	
	public VideoEffect() {
		// TODO Auto-generated constructor stub
	}
	
	
	public Effect createEffect (String EFFECT_SEPIA){
		
		
		return sepia; 
		
	}

	**/
	
	
}
